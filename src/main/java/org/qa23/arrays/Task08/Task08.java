package org.qa23.arrays.Task08;

import java.util.Random;

public class Task08 {
    public static void main(String[] args) {
        /*Random random = new Random();
        int[] arrays = new int[50];
        for (int i = 0; i < arrays.length; i++) {
            arrays[i] = random.nextInt(-100,100);
        }
        int min = 0;
        int max = 0;
        int countPositiveElement = 0;
        int countNegativeElement = 0;
        int countZeroElement = 0;
        for (int a = 0; a < arrays.length; a++) {
            System.out.print(arrays[a] + " ");
            if (arrays[a] > 0) {
                countPositiveElement++;
            }
            if (arrays[a] < 0) {
                countNegativeElement++;
            }
            if (arrays[a] == 0) {
                countZeroElement++;
            }

            if (arrays[a] > max) {
                max = arrays[a];
            }
            if (arrays[a] < min) {
                min = arrays[a];
            }
        }
        System.out.println("Количество нулей " + countZeroElement);
        System.out.println("Количество елементов больше 0 = " + countPositiveElement);
        System.out.println("Количество елементов меньше 0 = " + countNegativeElement);
        System.out.println("Максимальные элемент = " + max);
        System.out.println("Минимальный елемент  = " + min);*/
    }
}
